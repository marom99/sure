{
  "name": "sure-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for Sure Personal Finance – REST API v1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --experimental-strip-types --test test/**/*.test.mjs",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.1",
    "agents": "^0.0.80",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250321.0",
    "typescript": "^5.8.2",
    "wrangler": "^4.8.0"
  }
}
